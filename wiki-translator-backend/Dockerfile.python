FROM python:3.10-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir torch transformers sentencepiece fastapi uvicorn
COPY translator_app.py .
CMD ["uvicorn", "translator_app:app", "--host", "0.0.0.0", "--port", "8000"]
